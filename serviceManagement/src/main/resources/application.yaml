server:
  port: 9198
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Service-Request
    driverClassName: org.postgresql.Driver
    username: postgres
    password: admin

  jpa:
    showSql: true
    hibernate:
      ddl-auto: update
    databasePlatform: org.hibernate.dialect.PostgreSQLDialect

  application:
    name : Service-Request